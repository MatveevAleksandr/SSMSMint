<UserControl x:Class="SSMSMint.MixedLangInScriptWordsCheck.Views.MixedLangCheckToolWindowControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SSMSMint.MixedLangInScriptWordsCheck.Views"
             xmlns:viewmodels="clr-namespace:SSMSMint.MixedLangInScriptWordsCheck.ViewModels"
             d:DataContext="{d:DesignInstance Type=viewmodels:MixedLangCheckToolWindowViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="300">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0"
                   FontSize="20"
                   TextWrapping="Wrap"
                   FontWeight="SemiBold"
                   HorizontalAlignment="Center"
                   TextAlignment="Center">
            Words with a combination of
            <Run Text="Russian" Foreground="Red"/>
            and
            <Run Text="English" Foreground="Blue"/>
            letters were found
        </TextBlock>
        <Separator Height="10" Grid.Row="1"/>
        <TextBlock 
            Grid.Row="2" HorizontalAlignment="Right">
            <Run Text="Total: "/>
            <Run Text="{Binding MixedLangWordsCount, Mode=OneWay}"/>
        </TextBlock>
        <ListView 
            Grid.Row="3"
            ItemsSource="{Binding MixedLangWords}"
            Margin="0,10,0,0"
            MouseDoubleClick="MixedLangWordDoubleClickAsync">
            <ListView.Resources>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                </Style>
            </ListView.Resources>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <local:MixedLangWordControl 
                        LineIndex="{Binding LineIndex}"
                        ColumnIndex="{Binding ColumnIndex}"
                        Word="{Binding Word}"/>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</UserControl>
