<UserControl x:Class="SSMSMint.Core.UI.View.MixedLangCheckControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SSMSMint.Core.UI.View" 
             xmlns:viewmodels="clr-namespace:SSMSMint.Core.UI.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="300">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0"
                   FontSize="20"
                   TextWrapping="Wrap"
                   FontWeight="SemiBold"
                   HorizontalAlignment="Center"
                   TextAlignment="Center">
            Обнаружены слова с сочетанием
            <Run Text="русских" Foreground="Salmon"/>
            и
            <Run Text="английских" Foreground="SkyBlue"/>
            букв
        </TextBlock>
        <Separator Height="10" Grid.Row="1"/>
        <TextBlock 
            Grid.Row="2" HorizontalAlignment="Right">
            <Run Text="Total: "/>
            <Run Text="{Binding MixedLangWordsCount, Mode=OneWay}"/>
        </TextBlock>
        <ListView 
            Grid.Row="3"
            ItemsSource="{Binding MixedLangWords}"
            Margin="0,10,0,0"
            SelectionChanged="MixedLangWordItemSelectionChanged">
            <ListView.Resources>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                </Style>
            </ListView.Resources>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <local:MixedLangWordControl 
                        LineIndex="{Binding StartPoint.Line}"
                        ColumnIndex="{Binding StartPoint.Column}"
                        Word="{Binding Word}"/>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</UserControl>
